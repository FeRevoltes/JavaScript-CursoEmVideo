var velocidade = 80;
console.log(`Velocidade: ${velocidade}km/h, limite 60km/h`);

if (velocidade > 60) { // condição
  console.log(`Velocidade acima do limite!`);
} else {
  console.log(`velocidade abaixo do limite!`);
}
